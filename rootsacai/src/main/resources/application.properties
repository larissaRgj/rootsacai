# Configuração do Spring Boot

spring.application.name=rootsacai
server.port=8080
server.servlet.context-path=/

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# H2 Database (Desenvolvimento)
spring.datasource.url=jdbc:h2:mem:rootsacaidb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (opcional - para visualizar dados)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging
logging.level.root=INFO
logging.level.com.rootsacai=DEBUG
logging.level.org.springframework.web=DEBUG

# Servidor de produção (descomentar quando implementar)
# spring.datasource.url=jdbc:mysql://localhost:3306/rootsacai
# spring.datasource.username=root
# spring.datasource.password=sua_senha
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
